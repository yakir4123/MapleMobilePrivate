package com.bapplications.maplemobile.game;

import com.bapplications.maplemobile.pkgnx.NXNode;

public class MapBackgrounds {

    private boolean black;

    public MapBackgrounds(NXNode src)
    {
        int no = 0;
//        NXNode back = src.getChild("" + no);
//
//        while (back.getChildCount() > 0)
//        {
//            boolean front = ((Long)back.getChild("front").get()) > 0;

//            if (front)
//                foregrounds.push_back(back);
//            else
//                backgrounds.push_back(back);

//            no++;
//            back = src.getChild("" + no);
//        }
//
//        black = src.getChild("0").getChild("bS").get().equals("");
    }

    public MapBackgrounds() {}
}
